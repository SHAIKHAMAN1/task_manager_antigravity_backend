# Backend environment for Docker
# Secrets (MONGODB_URI, JWT_SECRET) are injected via docker-compose environment block
# and should NEVER be committed — keep them in server/.env locally.

PORT=5000
NODE_ENV=production
CLIENT_URL=http://localhost:5173
